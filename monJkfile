pipeline {
    agent any 
            stages {
                stage('Build') {
                    steps {
                        echo "build stage..."
                        sh 'make main'
                        sh 'ls'
                        archiveArtifacts artifacts: 'main', fingerprint: true, 
            }
        }  
    }
}
