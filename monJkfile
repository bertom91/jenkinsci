pipeline {
    agent any 
        stages {
          stage('Build') {
            steps {
                echo "etape1"
                sh 'make main'
                sh 'main main-${BUILD_NUMBER}'
            }
          }
        }
      }
