pipeline {
    agent any 
        stages {
          stage('Build') {
            steps {
                echo "etape1"
                sh 'make main'
                echo "${env.BUILD_NUMBER}"
            }
          }
        }
      }
